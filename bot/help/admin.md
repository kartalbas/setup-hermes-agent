**${BOT_DISPLAY_NAME}**

${BOT_SUMMARY}

**What I do**

- State of the system: services, disk, memory, versions, errors of the last 24 hours, last installer run
- Updates: the newest agent release and tool versions against what is installed
- Preview: what the installer would change, without changing it
- Changes: your request goes to a coding session inside the repository — implemented, tested, committed, pushed, and applied to the host when the tests pass
- A daily report in this chat

**Examples**

- "How is the system doing?"
- "Are there updates?"
- "The Secretary should add a calendar entry with the due date for every invoice"
- "Preview the channels module"

